import StarterKit from "@tiptap/starter-kit";

import plainTextStarterKitOptions from "./plain_text_starter_kit_options";

const extension = StarterKit.configure(plainTextStarterKitOptions);
export default extension;
