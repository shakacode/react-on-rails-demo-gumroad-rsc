name: Deploy Review App to Control Plane

run-name: "Deploy Review App - PR #${{ github.event.pull_request.number || github.event.issue.number || github.event.inputs.pr_number }}"

on:
  pull_request:
    types: [opened, synchronize, reopened]
  issue_comment:
    types: [created]
  workflow_dispatch:
    inputs:
      pr_number:
        description: Pull request number to deploy
        required: true
        type: number

permissions:
  contents: read
  deployments: write
  issues: write
  pull-requests: write

jobs:
  validate-review-app-source:
    if: |
      (github.event_name == 'pull_request' &&
       github.event.pull_request.head.repo.full_name == github.repository) ||
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'issue_comment' &&
       github.event.issue.pull_request &&
       contains(fromJson('["+review-app-deploy","+review-app-deploy\n","+review-app-deploy\r\n"]'), github.event.comment.body) &&
       contains(fromJson('["OWNER","MEMBER","COLLABORATOR"]'), github.event.comment.author_association))
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    outputs:
      deployable: ${{ steps.validate.outputs.deployable }}
      app_name: ${{ steps.validate.outputs.app_name }}
    steps:
      - id: validate
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          EVENT_NAME: ${{ github.event_name }}
          HEAD_REPO: ${{ github.event.pull_request.head.repo.full_name || '' }}
          PR_NUMBER: ${{ github.event.pull_request.number || github.event.issue.number || github.event.inputs.pr_number }}
        run: |
          set -euo pipefail
          app_name="react-on-rails-demo-gumroad-rsc-review-pr-${PR_NUMBER}"
          echo "app_name=${app_name}" >> "$GITHUB_OUTPUT"

          if [ "$EVENT_NAME" = "pull_request" ]; then
            resolved_head_repo="$HEAD_REPO"
          else
            resolved_head_repo="$(gh pr view "$PR_NUMBER" --json headRepository --jq '.headRepository.nameWithOwner')"
          fi

          if [ "$resolved_head_repo" = "$GH_REPO" ]; then
            echo "deployable=true" >> "$GITHUB_OUTPUT"
          else
            echo "deployable=false" >> "$GITHUB_OUTPUT"
            echo "::notice::Skipping review-app deploy for fork PR #${PR_NUMBER} from ${resolved_head_repo}."
          fi

  prepare-control-plane-db-secrets:
    needs: validate-review-app-source
    if: needs.validate-review-app-source.outputs.deployable == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Install Control Plane CLI
        run: npm install -g @controlplane/cli@3.10.2
      - name: Prepare app database secrets
        env:
          CPLN_TOKEN: ${{ secrets.CPLN_TOKEN_STAGING }}
          CPLN_ORG: shakacode-open-source-examples-staging
          APP_NAME: ${{ needs.validate-review-app-source.outputs.app_name }}
        run: bin/prepare-control-plane-db-secrets "$APP_NAME" --org "$CPLN_ORG"

  deploy:
    needs:
      - validate-review-app-source
      - prepare-control-plane-db-secrets
    if: needs.validate-review-app-source.outputs.deployable == 'true'
    uses: shakacode/control-plane-flow/.github/workflows/cpflow-deploy-review-app.yml@v5.1.1
    secrets:
      CPLN_TOKEN_STAGING: ${{ secrets.CPLN_TOKEN_STAGING }}
      DOCKER_BUILD_SSH_KEY: ${{ secrets.DOCKER_BUILD_SSH_KEY }}
